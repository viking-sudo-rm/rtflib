rtflib -- A Python bridge to writing RTFs.

Dependencies:
Almost none (except Python, of course).

Configuration:
As is, the module can be accessed by any other file in the same directory.  To add it to the global Python path, type this in the Python command-line interpreter:
import sys
sys.path.append("THE FILEPATH TO rtflib.py")